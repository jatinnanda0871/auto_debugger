# auto_debugger
A python based project that can read through memory dump and pinpoint module where failure happened based on memory mata-data (pointing region of interest) and running analyzer test cases on those region of interest. This script also provide replay feature and showing memory range requested by user similar to gdb/t32
